{{#if postInfo}}

  <section class="content">
    <h1 class="title">Edit your post</h1>
    <form class="specForm" action="/posts/{{postInfo.title}}/edit" method="POST">
      <label for="title">Title</label>
      <input
        type="text"
        value="{{postInfo.title}}"
        id="title"
        name="title"
        maxlength="30"
        minlength="5"
        required
      />
      <label for="topic">Topic</label>
      <input
        type="text"
        id="topic"
        value="{{postInfo.topic}}"
        name="topic"
        minlength="3"
        maxlength="15"
        required
      />
      <label for="url">Link to study material</label>
      <input
        type="text"
        value="{{postInfo.url}}"
        minlength="10"
        name="url"
        required
      />
      <label for="description">Description</label>
      <textarea
        name="description"
        id="description"
        minlength="10"
        maxlength="200"
      >{{postInfo.description}}</textarea>
      <button id="postB" type="submit">Proceed </button>
      <button id="checkIcon" type="submit"><i class="fa-solid fa-check"></i></button>
    </form>

  </section>

{{else}}
  <section class="content">
    <h1 class="title">Contribute to Note Exchange</h1>
    <form class="specForm" action="/postResource" method="POST">
      <label for="title">Title</label>
      <input
        type="text"
        id="title"
        name="title"
        maxlength="30"
        minlength="5"
        required
      />
      <label for="topic">Topic</label>
      <input
        type="text"
        id="topic"
        name="topic"
        minlength="3"
        maxlength="15"
        required
      />
      <label for="url">Link to study material</label>
      <input type="text" minlength="10" name="url" required />
      <label for="description">Description</label>
      <textarea
        name="description"
        id="description"
        minlength="10"
        maxlength="200"
      ></textarea>
      <button id="postB" type="submit">Post </button>
      <button id="checkIcon" type="submit"><i class="fa-solid fa-check"></i></button>
      
    </form>

  </section>

{{/if}}